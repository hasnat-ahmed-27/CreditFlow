# Service-specific deps only. The shared `creditflow_common` package (and its
# deps: pyjwt[crypto], pika, sqlalchemy, psycopg, redis) is installed from
# /libs in the Dockerfile.
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
# Task queue + beat — Redis as broker AND result backend (spec).
celery[redis]==5.4.0
# IANA timezone database for zoneinfo — present on Linux images, but required
# on Windows dev machines and slim CI runners for the recurrence math.
tzdata>=2024.1
# Test-only: FastAPI's TestClient transport (CI installs this file).
httpx>=0.27
# Test-only: in-memory Redis so pytest runs without infra (CI installs this file).
# >=2.26 supports the RESP3 HELLO handshake that modern redis-py sends on connect;
# covers the fire lock (Celery itself runs eager in tests and never touches a broker).
fakeredis>=2.26.0

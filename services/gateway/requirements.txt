# The gateway is a pure reverse proxy — deliberately NO creditflow_common:
# no DB, no broker, no Redis, and no JWT handling (services verify their own
# RS256 tokens; the Authorization header passes through untouched).
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
# Streaming upstream client — the SSE passthrough needs chunk-by-chunk reads.
httpx>=0.27

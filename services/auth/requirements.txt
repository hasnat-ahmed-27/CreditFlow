# Service-specific deps only. The shared `creditflow_common` package (and its
# deps: pyjwt, pika, sqlalchemy, psycopg, redis) is installed from /libs in the
# Dockerfile.
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
# Password hashing (spec: bcrypt or argon2 via passlib — argon2 chosen).
passlib==1.7.4
argon2-cffi==23.1.0
# Pydantic EmailStr validation for signup/login payloads.
email-validator==2.1.1
# Outbound HTTP: user_client.py resolves the account_id/role claims from the
# User service (and the TestClient transport).
httpx>=0.27
# Test-only: in-memory Redis so pytest runs without infra (CI installs this file).
# >=2.26 supports the RESP3 HELLO handshake that modern redis-py sends on connect
# (older fakeredis fails with: unknown command `hello`).
fakeredis>=2.26.0
